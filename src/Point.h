#pragma once
#ifndef POINT_H
#define POINT_H

class Point {
    private:
        // TODO: Make point variables private
    public:
        int x;
        int y;

        Point(int x, int y);
        ~Point();
};

#endif
